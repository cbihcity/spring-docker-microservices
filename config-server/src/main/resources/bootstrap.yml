server:
  port: 8888
service:
    name: CONFIG-SERVER
spring:
  application:
    name: configserver
  cloud:
    config:
      fail-fast: true
      server:
        git:
          uri: https://github.com/cbihcity/spring-boot-microservice-config
          search-paths: configs